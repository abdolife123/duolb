---
interface Props {
  topCities: { name: string; slug: string }[];
  topCategories: { name: string; slug: string }[];
}

const { topCities, topCategories } = Astro.props;
---

<section class="mt-16 rounded-3xl border border-[var(--color-light-taupe)] bg-[var(--color-warm-cream)] p-8 md:p-10" aria-labelledby="home-seo">
  <h2 id="home-seo" class="text-2xl md:text-3xl font-serif text-slate-900">
    Beauty & Wellness Anbieter in Deutschland vergleichen
  </h2>
  <p class="mt-4 text-slate-700 leading-relaxed">
    Duolb ist dein Verzeichnis für Schönheit und Wellness in Deutschland. Hier findest du Friseure,
    Kosmetikstudios, Nagelstudios, Brow- und Lash-Studios sowie Wellnessanbieter in deiner Nähe.
    Alle Einträge sind übersichtlich nach Stadt und Kategorie organisiert, damit du schnell die passenden Anbieter
    vergleichen kannst.
  </p>
  <ul class="mt-4 list-disc pl-5 text-slate-700 leading-relaxed">
    <li>Bewertungen und Anzahl der Rezensionen auf einen Blick</li>
    <li>Adresse, Website und Kontaktinformationen direkt erreichbar</li>
    <li>Interne Verlinkung nach Stadt und Kategorie für kurze Wege</li>
  </ul>

  <div class="mt-8 grid gap-6 md:grid-cols-2">
    <div>
      <h3 class="text-sm font-semibold uppercase tracking-[0.25em] text-slate-500">
        Häufige Städte
      </h3>
      <p class="mt-2 text-sm text-slate-600">
        {topCities.map((c, idx) => (
          <>
            <a
              class="text-[var(--color-terracotta)] transition-colors hover:text-[var(--color-gentle-gold)] hover:underline"
              href={`/directory/city/${c.slug}`}
            >{c.name}</a>
            {idx < topCities.length - 1 ? ", " : ""}
          </>
        ))}
      </p>
    </div>
    <div>
      <h3 class="text-sm font-semibold uppercase tracking-[0.25em] text-slate-500">
        Häufige Kategorien
      </h3>
      <p class="mt-2 text-sm text-slate-600">
        {topCategories.map((c, idx) => (
          <>
            <a
              class="text-[var(--color-terracotta)] transition-colors hover:text-[var(--color-gentle-gold)] hover:underline"
              href={`/directory/category/${c.slug}`}
            >{c.name}</a>
            {idx < topCategories.length - 1 ? ", " : ""}
          </>
        ))}
      </p>
    </div>
  </div>
</section>
